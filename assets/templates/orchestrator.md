# Model Routing

Read the **Model Assignments** table below at session start. Cache `phase → model` for the session and pass the mapped model in every `Agent` tool call via the `model` parameter.

- If a phase is missing from the table, use the `default` row.
- If access to the assigned model is unavailable, substitute `sonnet` and continue.
